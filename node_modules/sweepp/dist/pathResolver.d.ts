import { MonorepoConfig } from './monorepo';
/**
 * Resolve TypeScript/JavaScript path aliases from tsconfig.json or jsconfig.json
 */
export declare function resolvePathAliases(projectRoot: string): Map<string, string>;
/**
 * Resolve an import path that might contain an alias
 */
export declare function resolveImportPath(importPath: string, aliasMap: Map<string, string>): string;
/**
 * Check if an import is a relative/absolute path (not a node_modules package)
 */
export declare function isLocalImport(importPath: string): boolean;
/**
 * Resolve the full file path for an import statement
 */
export declare function resolveFullImportPath(importPath: string, currentFilePath: string, projectRoot: string, aliasMap: Map<string, string>, monorepoConfig?: MonorepoConfig): string | null;
