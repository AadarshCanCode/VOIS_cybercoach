import { MonorepoConfig } from './monorepo';
export interface RemovedImportInfo {
    source: string;
    specifiers: string[];
}
export interface ImportCleanupResult {
    filePath: string;
    removed: RemovedImportInfo[];
    originalImportDecls: number;
    finalImportDecls: number;
    changed: boolean;
    totalRemovedSpecifiers: number;
    newCode?: string;
}
export interface AnalyzeOptions {
    checkLocalImports?: boolean;
    projectRoot?: string;
    monorepoConfig?: MonorepoConfig;
}
export declare function analyzeAndClean(filePath: string, dryRun: boolean, options?: AnalyzeOptions): Promise<ImportCleanupResult>;
