export interface DiscoverOptions {
    root: string;
    extensions: string[];
    ignore: string[];
}
export declare function discoverFiles(patternOrDir: string, opts: DiscoverOptions): Promise<string[]>;
