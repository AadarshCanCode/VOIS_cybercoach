export interface WorkspacePackage {
    name: string;
    path: string;
    packageJson?: any;
}
export interface MonorepoConfig {
    type: 'pnpm' | 'npm' | 'yarn' | 'turborepo' | 'none';
    root: string;
    packages: WorkspacePackage[];
}
/**
 * Detect if the current directory is a monorepo and what type
 */
export declare function detectMonorepo(rootDir: string): MonorepoConfig;
/**
 * Check if a path is within any workspace package
 */
export declare function findPackageForFile(filePath: string, monorepoConfig: MonorepoConfig): WorkspacePackage | null;
/**
 * Resolve imports within monorepo (workspace protocol)
 */
export declare function resolveWorkspaceImport(importPath: string, currentPackage: WorkspacePackage | null, monorepoConfig: MonorepoConfig): string | null;
