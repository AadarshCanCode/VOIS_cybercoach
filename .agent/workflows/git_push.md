---
description: Push changes to git
---

1. Add all changes
2. Commit with descriptive message
3. Push to remote

// turbo-all
1. git add .
2. git commit -m "Fix course visibility, schema, and module count issues"
3. git push origin main
